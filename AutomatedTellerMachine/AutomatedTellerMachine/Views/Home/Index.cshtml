@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div class="col-md-6 margin_top_20">
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-circle-arrow-down"> Withdrawal</span></a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-flash"> Quick Cash $100</span></a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-question-sign"> Balance Enquiry</span></a>
    </div>
    <div class="col-md-6 margin_top_20">
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-print"> Print Statement</span></a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-transfer"> Transfer Funds</span></a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-circle-arrow-up"> Deposit</span></a>
    </div>
</div>
@*
    @Request.Browser.Browser and that's Okay!!

    @{ var amounts = new List<decimal>() { 100, 24.233m, -39 };}
    <ul>
        @foreach(decimal amount in amounts)
        {
            <li>@formatAmount(amount)</li>
        }
    </ul>
    @helper formatAmount(decimal amount){
        var color = "green";
        if (amount < 0) {
            color = "red";
        }
    }
    <span style="color:@color">@String.Format("{0:c}",amount)</span>
*@